<h1>Doctors</h1>

<p>Patient Count: <%= doc.patient_count %>

<ul>
  <% @doctors.each do |doc| %>
    <li>
      <%= link_to doc.name, doctor_path(doc) %></a>
      <%= doc.patient_count %>
    </li>
  <% end %>
</ul>
